
'use client';

import React from 'react';
// Removed imports for Button, Avatar, DropdownMenu, Input, Label, useAuth, Loader2, LogIn as they are no longer used for login UI

export function UserProfileMenu() {
  // Login functionality and UI has been removed. This component now renders nothing.
  return null;
}
